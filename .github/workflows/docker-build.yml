name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: atom
            build_dir: build-atom
            install_dir: install-atom
          - platform: qemu
            build_dir: build-qemu
            install_dir: install-qemu
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build Docker image
        run: docker build -f ci/Dockerfile.debian -t pbus-build .

      # build_internal.sh, not build.sh
      - name: Run build script
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            --user $(id -u):$(id -g) \
            --cap-add=NET_RAW \
            pbus-build bash ci/build_internal.sh --platform=${{ matrix.platform }}

      - name: Run unit tests
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            --user 0:0 \
            --cap-add=NET_RAW \
            pbus-build ./${{ matrix.build_dir }}/src/tests/unit_tests

      - name: Prepare artifact name
        id: prep
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_RUN_NUMBER: ${{ github.run_number }}
        run: |
          BRANCH="${GITHUB_REF_NAME//\//-}"
          DATE=$(date +%Y%m%d)
          echo "file=pbus-${{ matrix.platform }}-${BRANCH}-${DATE}-${GITHUB_RUN_NUMBER}.tar.gz" >> "$GITHUB_OUTPUT"

      - name: Package install directory
        run: tar -czf "${{ steps.prep.outputs.file }}" -C ${{ matrix.install_dir }} .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prep.outputs.file }}
          path: ${{ steps.prep.outputs.file }}
